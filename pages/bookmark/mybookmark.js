import PleaseSignIn from "../../components/auth/PleaseSignIn";

const Bookmark = props => (
    <div>
        <PleaseSignIn>
                BOOK MARK
        </PleaseSignIn>
    </div>
);

export default Bookmark;
